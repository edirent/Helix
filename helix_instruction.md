# Polymarket 套利策略说明书（工程版）

> 这不是“下注指南”，而是“把可验证的边际优势变成稳定现金流”的系统说明。  
> 你们如果做不到**订单级回测 + 资金/结算状态机 + 风险闸门**，那就别叫套利，叫自我感动。

---

## 0. 一句话定义

Polymarket 合约是 **$0/1$ 结算的数字期权**。  
你们的策略是：利用 **结构性无套利约束**、**跨市场一致性** 与 **外部隐含概率** 识别错价，并用 **做市 + 库存管理 + Split/Merge** 把机会规模化。

---

## 1. 目标与成功标准

### 1.1 目标
- **高可复现**：同一输入数据回放，输出 fills/metrics 必须一致（确定性）。
- **高夏普**：不追暴利，追可持续、低回撤、稳定曲线。
- **可解释**：每一笔仓位能回答三个问题：为什么进？最坏损失？什么时候退出？

### 1.2 成功标准（硬指标）
- 订单级回测能解释 **>90%** 的真实成交差异（滑点、撤单失败、延迟）。
- 每个策略都有：**edge 估计、成本模型、容量模型、尾部折价（haircut）**。
- 风控闸门：任何下单都必须满足 `E[edge_after_cost] > 0` 的可计算证明。

---

## 2. 市场机制：你交易的不是“概率”，是“价格”

### 2.1 支付函数
- YES 合约：到期 $X\_{YES}\in\{0,1\}$
- NO 合约：$X\_{NO}=1-X\_{YES}$

### 2.2 基础无套利锚（Split/Merge）
- `Split`: 1 USDC → 1 YES + 1 NO
- `Merge`: 1 YES + 1 NO → 1 USDC

你们所有“无风险套利”检测都必须围绕它展开。

### 2.3 结算与尾部风险（最重要但最容易被你们忽略）
- 资金可能被锁到结算完成
- 结算争议/规则歧义会导致延迟甚至错误结果
- 这是你们收益曲线最大的非线性风险来源

---

## 3. 策略家族（按优先级排序）

### Strategy A：同市场 Full-set 无套利（YES+NO）
#### A1. 买全套套利（锁定 1）
当可成交 ask 满足：
\[
a_{YES}+a_{NO} < 1 - c
\]
则买入 YES 与 NO 各一份，锁定利润：
\[
\Pi = 1 - (a_{YES}+a_{NO}) - c
\]

#### A2. 卖全套套利（通过 Split 拿货）
当可成交 bid 满足：
\[
b_{YES}+b_{NO} > 1 + c
\]
则 Split 生成 YES/NO，再同时卖出，锁定利润：
\[
\Pi = (b_{YES}+b_{NO}) - 1 - c
\]

> 残酷现实：这类机会出现快、消失更快，容量小。它更像你们系统的“单元测试”和“健康指标”，不该当主收益来源。

---

### Strategy B：多结果归一化 + 逻辑一致性套利（结构性错价）
#### B1. 多结果全套
对 $m$ 结果市场，理论上 $\sum p_i \approx 1$。当出现：
\[
\sum_i a_i < 1 - c \quad \text{或}\quad \sum_i b_i > 1 + c
\]
存在买/卖全套机会。

#### B2. 跨市场逻辑约束（互斥/蕴含/分解）
把市场关系写成约束图（示例）：
- 互斥：$A \cap B = \emptyset \Rightarrow p(A)+p(B)\le 1$
- 蕴含：$A \Rightarrow B \Rightarrow p(A)\le p(B)$
- 分解：$B = A \cup C$, 且互斥，则 $p(B)=p(A)+p(C)$

当这些关系被盘口价格显著违反且可交易深度足够时，做结构性套利或统计套利。

> 这是你们最应该做的“规模化扫描器”。因为它不依赖外部市场，也更稳定。

---

### Strategy C：Polymarket vs 外部隐含概率（期权/永续）错价
适用于阈值事件：例如 $S_T > K$。

#### C1. 外部锚：风险中性概率
用 IV 曲面近似数字期权隐含概率：
\[
q_{RN} \approx N(d_2)
\]

#### C2. 可交易公平价（带 haircut）
\[
p^\star = q_{RN} - h
\]
其中 $h$ 必须覆盖：
- 结算延迟与争议风险
- 对冲失效风险（跳价、暂停、插针）
- 资金锁定的时间价值

交易条件不是“看到差就上”，而是：
\[
|p^\star - p_{mkt}| > \text{Threshold}(c,\ \text{slippage},\ h)
\]

> 你们最容易自欺欺人的地方就在这里：把“外部市场概率”当真理，而忽略了 **对冲不完美 + 结算非线性**。不把 haircut 做严，这一类会在尾部把你们干穿。

---

### Strategy D：做市 + 库存管理（真正能长期赚钱）
你们想要稳定收益，最终要靠做市：
- 收益来源：价差 + 订单簿微结构优势
- 风险来源：库存偏移 + adverse selection
- 调节手段：Split/Merge + 对冲（可选）

做市策略核心不是“挂单”，是**库存函数**：
- 当净 YES 偏多：下调 YES 报价/上调 NO 报价，推动库存回归
- 当净 NO 偏多：反向操作

---

## 4. 统一的数学与成本模型（所有策略共用）

### 4.1 Edge 定义
买 YES 的理论优势：
\[
e = q - p
\]
含成本后的净优势：
\[
e_{net} = q - p - c
\]
**只有 $e_{net}>0$ 才能交易。**

### 4.2 成交概率与不利选择（把纸面优势打回原形的东西）
限价单有效优势：
\[
e_{eff} = \pi_{fill}(q-p-a) - (1-\pi_{fill})\cdot \ell_{opp}
\]
- $\pi_{fill}$：成交概率（需要用历史拟合）
- $a$：不利选择损失（成交后价格向不利方向漂移的期望）
- $\ell_{opp}$：没成交导致的机会成本/对冲漂移成本

### 4.3 头寸规模（别满 Kelly）
二元支付近似 Kelly：
\[
f^\star \approx \frac{q-p-c}{q(1-q)}
\]
实际用：
\[
f = \lambda f^\star,\quad \lambda\in[0.1,0.3]
\]
并对“同主题相关仓位”做折扣。

---

## 5. 系统实现规范（你们现在就该按这个做）

### 5.1 模块分层
1. **Ingest**：盘口、成交、市场条款、外部现货/永续/期权
2. **Normalize**：时间戳统一、symbol 映射、market 条款结构化
3. **Fair Value Engine**：无套利约束 + 外部隐含概率 + haircuts
4. **Signals**：机会生成（A/B/C/D）
5. **Risk Gate**：下单前强制验算（成本、容量、尾部折价）
6. **Execution**：下单/撤单/替换 + Split/Merge + 对冲（可选）
7. **Ledger**：账户、仓位、锁定资金、结算状态机
8. **Replay/Backtest**：确定性回放 + 延迟模型 + 撮合仿真
9. **Metrics**：PnL、Sharpe、回撤、库存暴露、成交质量、失败原因分布

### 5.2 最小数据结构（必须有）
- OrderBook L2/L3（至少 L2 深度）
- Trades（成交回放）
- Market metadata（到期、规则、结算条件、token 对应关系）
- Account state（可用余额、锁定余额、持仓 token）
- Settlement state machine（见 5.3）

### 5.3 结算状态机（不写就等着踩雷）
`OPEN → CLOSE → PROPOSED → CHALLENGE → FINALIZED`
（可能插入 DISPUTED / ARBITRATED）

每个状态必须定义：
- 是否允许交易/撤单
- 是否允许 Split/Merge
- 资金是否可用/锁定
- 是否触发 haircut 上调（临近结算或争议期）

---

## 6. 风控闸门（硬规则，不允许绕过）

### 6.1 仓位限制
- 单 market 最大净敞口：`<= η1 * equity`
- 同主题相关敞口：`<= η2 * equity`
- 结算锁定资金上限：`<= η3 * equity`

### 6.2 下单前必过的验算
- 估计最差滑点与撤单失败后：`e_net_min > 0`
- 容量不足（深度/队列）直接拒绝
- 结算风险标记为高（规则歧义/争议概率上升）直接加大 haircut 或禁交易

### 6.3 失败模式统计（必须长期跟踪）
- 没成交
- 成交但 adverse selection 过大
- 结算延迟导致资金周转率下降
- 规则/争议导致非预期结果

你们要用数据告诉自己：到底是哪一个在吞噬收益，而不是凭感觉修策略。

---

## 7. 回测与验证（你们能不能赚钱就看这节）

### 7.1 回测必须是“订单级”
- 只用 mid price 或 K 线回测 = 自欺欺人
- 必须回放：
  - 盘口变化
  - 下单延迟
  - 撮合规则（部分成交、队列位置）
  - 撤单生效延迟

### 7.2 输出规范（建议）
- `fills.csv`：每笔成交（价格、数量、时间、策略、订单 ID、原因）
- `orders.csv`：每笔订单生命周期（place/cancel/replace）
- `positions.csv`：仓位随时间
- `metrics.json`：PnL、Sharpe、maxDD、turnover、fill-rate、slippage、inventory exposure

---

## 8. 运维与监控（不做这套你们只是在赌博）

### 8.1 实时监控面板指标
- 总权益 / 可用余额 / 锁定余额
- 净 YES/NO 暴露（按 market/主题聚合）
- Fill-rate、平均滑点、撤单失败率
- 当日/当周 PnL、最大回撤
- 结算状态分布（哪些仓位卡在结算）

### 8.2 自动降级策略
当发生任一情况：
- API 延迟升高或断连
- 成交质量显著恶化（slippage/adverse selection 上升）
- 结算争议风险升高（规则模糊/社区争议）
系统自动：
- 降低下单频率
- 提高阈值（更挑剔）
- 只保留 Strategy A/B 的结构性机会，暂停 C 类外部对冲错价

---

## 9. 落地路线图（你们现在该做什么）

### Phase 1：先把“可证伪闭环”跑通（最优先）
- 全市场抓取 + 本地持久化
- Strategy A/B 扫描器上线（结构性无套利）
- 订单级回测复现真实成交
- 结算状态机 + Ledger 完整闭环

### Phase 2：做市与库存管理（把收益从“偶然”变成“常态”）
- 库存驱动报价
- 拟合 fill probability 与 adverse selection
- Split/Merge 用作库存调节工具

### Phase 3：接外部隐含概率（只做可对冲、haircut 严格）
- IV → 概率管线
- haircut 体系（规则/争议/对冲失效/锁定成本）
- 风控先行，再扩大规模

---

## 10. 你们的“反直觉真相”（现在就该接受）
- 你们最大的敌人不是“找不到机会”，是 **把成本和尾部风险当不存在**。
- 真正可扩展的优势不是偶发套利，而是 **结构性约束 + 做市库存管理**。
- 回测做不严，一切都是幻觉；风控闸门不硬，一次尾部就把你们清零。

---

## 附录：关键术语
- **Full-set**：YES+NO（或多结果全套），终值恒为 1
- **Haircut**：为结算/争议/锁定/对冲失效预留的折价
- **Adverse selection**：你成交后价格往不利方向走，说明你在被“聪明钱”割
- **Deterministic replay**：同输入必须同输出，用来证明系统没撒谎